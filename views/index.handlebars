<h1>Burgers</h1>

<h2>Burgers that are available!</h2>
<ul>
  {{#each burgers}}
    <li>
      <p> Burger Name: {{burger_name}} </p>
      <p> Burger ID: {{id}} </p>
      <p> Burger Status: {{devoured}} </p>
   </li>
  {{/each}}
</ul>

<h2>Burgers that are not available!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{burger_name}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burgerName" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>
